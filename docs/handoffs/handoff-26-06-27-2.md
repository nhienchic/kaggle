# Handoff: Kaggle Capstone Submission Coach Runtime + Next Dashboard Slice

## Current State

The project has progressed from the first runnable Streamlit slice through live Gemini adapter wiring.

Recent committed work:

- `e437161` add downloadable submission artifacts
- `37bacbd` add repository security scanning
- `844bada` add mcp-compatible analysis tools
- `bc858e5` add model runtime fallback path
- `fd71a73` wire live gemini adapter

The working tree was clean when this handoff was written.

Core local artifacts to reference:

- PRD: `docs/cross-agent-docs/prd-kaggle-capstone-submission-coach.md`
- Prior handoff: `docs/handoffs/handoff-26-06-27-1.md`
- Coding reports:
  - `docs/cross-agent-docs/coding-report-issue-2-first-readiness-report.md`
  - `docs/cross-agent-docs/coding-report-issue-3-downloadable-artifacts.md`
  - `docs/cross-agent-docs/coding-report-issue-4-security-scanning.md`
  - `docs/cross-agent-docs/coding-report-issue-5-mcp-tools.md`
  - `docs/cross-agent-docs/coding-report-issue-6-gemini-runtime-fallback.md`
  - `docs/cross-agent-docs/coding-report-gemini-live-adapter.md`
- Main code:
  - `app.py`
  - `kaggle_capstone_coach/workflow.py`
  - `kaggle_capstone_coach/mcp_tools.py`
  - `kaggle_capstone_coach/security.py`
  - `kaggle_capstone_coach/gemini_adapter.py`
- Tests:
  - `tests/test_readiness_workflow.py`
  - `tests/test_mcp_tools.py`
  - `tests/test_security_scanner.py`
  - `tests/test_gemini_adapter.py`

## Runtime Notes

The user created a Google AI Studio key. Do not print, commit, or ask them to paste the key.

Environment variable behavior:

- App accepts `GEMINI_API_KEY` or `GOOGLE_API_KEY`.
- `app.py` prefers `GEMINI_API_KEY` when building `GeminiModelAdapter`.
- Deterministic fallback remains available when no key is configured.
- If Gemini errors, `workflow.py` falls back to deterministic report output with `model_mode == "model-error-fallback"` and exposes `model_error`.

Live verification completed:

- Low-level Gemini SDK smoke returned `API OK`.
- App helper smoke returned `model-backed`, no model error, and four agent summaries.
- A transient provider-side `503` and a quota-style `429` were observed during testing; the fallback path was added after that.

Streamlit:

- Running at `http://localhost:8501`
- Last known PID: `2076`
- Stop with `Stop-Process -Id 2076` if needed.

Verification at handoff:

```powershell
python -m unittest discover -s tests
```

Result: 22 tests passed.

## Next Work

Next ready issue:

- Issue #7: https://github.com/nhienchic/kaggle/issues/7
- Title: Add judge-facing scoring and concept evidence dashboard

Issue #7 asks for a dashboard that makes judge-visible scoring evidence obvious:

- Rubric readiness grouped by pitch, implementation, and documentation.
- Evidence map for multi-agent/ADK, MCP, security, and deployability.
- Required submission assets with present/partial/missing status.
- Prioritized next steps focused on scoring evidence.
- Tests proving dashboard data is derived from the structured report.

Issue #8 follows after #7:

- Issue #8: https://github.com/nhienchic/kaggle/issues/8
- Title: Package the project for submission and deployment

## Suggested Approach

For issue #7, keep the same pattern:

1. Add a report-level behavior test for dashboard/scoring data first.
2. Add a small structured output, likely on `ReadinessReport`, such as `dashboard()` or `scoring_dashboard()`.
3. Derive dashboard data from existing checklist, security summary, model mode, MCP/tool evidence, and current artifact evidence.
4. Render the new dashboard in Streamlit without moving analysis logic into `app.py`.
5. Avoid live Gemini calls in tests; pass `environment={}` or fake adapters.

Do not start with deployment packaging; that is issue #8 and is blocked by #7.

## Suggested Skills

- `tdd`: use vertical slices and report-level behavior tests.
- `karpathy-code-guideline`: keep the dashboard data model small and avoid speculative scoring complexity.
- `github:github`: fetch issue #7/#8 details if needed.
- `diagnosing-bugs`: use only if Streamlit, Gemini, or dependency setup fails unexpectedly.

## Safety Reminders

- Follow `AGENTS.md`: do not run destructive filesystem or git commands.
- Do not run `git add`; ask the user to stage/commit.
- Keep API keys out of files, logs, handoffs, commits, and screenshots.
- Ignore any `human_*` files or folders if they appear.
